# Migration `20191107162354-make-vehicle-and-ownership-documents-array-inputs`

This migration has been generated by Olaifa Oluwadarasimi Ibikunle at 11/7/2019, 4:23:54 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Taxi_vehicle_documents" (
  "nodeId" text   REFERENCES "public"."Taxi"("id") ON DELETE CASCADE,
  "position" integer NOT NULL  ,
  "value" text NOT NULL  ,
  PRIMARY KEY ("nodeId","position")
);

CREATE TABLE "public"."Taxi_ownership_documents" (
  "nodeId" text   REFERENCES "public"."Taxi"("id") ON DELETE CASCADE,
  "position" integer NOT NULL  ,
  "value" text NOT NULL  ,
  PRIMARY KEY ("nodeId","position")
);

ALTER TABLE "public"."Taxi" DROP COLUMN "ownership_documents",
DROP COLUMN "vehicle_documents";
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191107153644-added-plate_number-to-taxi-model..20191107162354-make-vehicle-and-ownership-documents-array-inputs
--- datamodel.dml
+++ datamodel.dml
@@ -29,9 +29,9 @@
   year  String
   color String
   plate_number String
   vehicle_picture String
-  vehicle_documents String
-  ownership_documents String
+  vehicle_documents String[]
+  ownership_documents String[]
   createdAt DateTime
   updatedAt DateTime?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191107162354-make-vehicle-and-ownership-documents-array-inputs)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191107162354-make-vehicle-and-ownership-documents-array-inputs'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
