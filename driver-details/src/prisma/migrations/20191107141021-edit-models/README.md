# Migration `20191107141021-edit-models`

This migration has been generated by Olaifa Oluwadarasimi Ibikunle at 11/7/2019, 2:10:21 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "public"."Taxi_ownership_documents";

DROP TABLE "public"."Taxi_vehicle_documents";

ALTER TABLE "public"."Driver" DROP COLUMN "taxi";

ALTER TABLE "public"."Taxi" ADD COLUMN "driver" text   REFERENCES "public"."Driver"("id") ON DELETE SET NULL,
ADD COLUMN "ownership_documents" text NOT NULL DEFAULT '' ,
ADD COLUMN "vehicle_documents" text NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191107135320-edit-driver-taxi-relationship..20191107141021-edit-models
--- datamodel.dml
+++ datamodel.dml
@@ -14,9 +14,9 @@
   lastName  String
   status  String
   phone_number  String
   drivers_license String
-  taxi Taxi
+  taxi Taxi[]
   trips Int @default(0)
   createdAt DateTime
   updatedAt DateTime?
 }
@@ -26,9 +26,9 @@
   manufacturer  String
   car_model String
   capacity  Int
   year  String
-  vehicle_documents String[]
-  ownership_documents String[]
+  vehicle_documents String
+  ownership_documents String
   createdAt DateTime
   updatedAt DateTime?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191107141021-edit-models)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191107141021-edit-models'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
