# Migration `20191104135424-make-updated-at-column-nullable`

This migration has been generated by Olaifa Oluwadarasimi Ibikunle at 11/4/2019, 1:54:24 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Driver" DROP COLUMN "updatedAt",
ADD COLUMN "updatedAt" timestamp(3)   ;

ALTER TABLE "public"."Taxi" DROP COLUMN "updatedAt",
ADD COLUMN "updatedAt" timestamp(3)   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191104033228-change-car-model-column-from-model-to-car_model..20191104135424-make-updated-at-column-nullable
--- datamodel.dml
+++ datamodel.dml
@@ -16,9 +16,9 @@
   phone_number  String
   drivers_license String
   trips Int @default(0)
   createdAt DateTime
-  updatedAt DateTime
+  updatedAt DateTime?
 }
 model Taxi {
   id  String  @default(cuid())  @id @unique
@@ -29,6 +29,6 @@
   year  String
   vehicle_documents String[]
   ownership_documents String[]
   createdAt DateTime
-  updatedAt DateTime
+  updatedAt DateTime?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191104135424-make-updated-at-column-nullable)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191104135424-make-updated-at-column-nullable'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
