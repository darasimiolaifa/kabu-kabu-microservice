# Migration `20191026191728-init`

This migration has been generated by Olaifa Oluwadarasimi Ibikunle at 10/26/2019, 7:17:28 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Passenger" DROP COLUMN "trips",
ADD COLUMN "trips" integer NOT NULL DEFAULT 0 ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191026184205-init..20191026191728-init
--- datamodel.dml
+++ datamodel.dml
@@ -13,15 +13,6 @@
   firstName  String
   lastName  String
   status  String
   createdAt DateTime
-  trips Int?
-}
-// model Post {
-//   id        String   @default(cuid()) @id @unique
-//   createdAt DateTime @default(now())
-//   updatedAt DateTime @updatedAt
-//   published Boolean
-//   title     String
-//   content   String?
-//   author    User?
-// }
+  trips Int @default(0)
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191026191728-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191026191728-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
