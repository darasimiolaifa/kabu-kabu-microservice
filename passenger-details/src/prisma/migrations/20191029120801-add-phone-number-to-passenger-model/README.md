# Migration `20191029120801-add-phone-number-to-passenger-model`

This migration has been generated by Olaifa Oluwadarasimi Ibikunle at 10/29/2019, 12:08:02 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Passenger" ADD COLUMN "phone_number" text NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191026191728-init..20191029120801-add-phone-number-to-passenger-model
--- datamodel.dml
+++ datamodel.dml
@@ -12,7 +12,8 @@
   email String  @unique
   firstName  String
   lastName  String
   status  String
+  phone_number  String
   createdAt DateTime
   trips Int @default(0)
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191029120801-add-phone-number-to-passenger-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191029120801-add-phone-number-to-passenger-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
